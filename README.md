# Markdown syntax guide

1. ``` gcc AirInterface.c -o AirInterface.exe -L. -liio -lm" ```, to compile ```AirInterface.c```.
2. Run this command:
   1. For linux: ```python3 ADS-BTerminal.py ```
   2. For Windows: ```python ADS-BTerminal.py ```